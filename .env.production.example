# Core ports
WEB_PORT=3000
WEBAPI_PORT=8080
WEBADMIN_PORT=8081
DOZZLE_PORT=9999

# Internal service connections
POSTGRES_DB=transcendence
POSTGRES_USER=postgres
POSTGRES_PASSWORD=CHANGE_ME_STRONG_DB_PASSWORD
REDIS_CONNECTION=redis:6379
RIOT_API_KEY=RGAPI-CHANGE_ME

# Auth settings (set secure values before production launch)
JWT_ISSUER=Transcendence
JWT_AUDIENCE=TranscendenceClients
JWT_SIGNING_KEY=CHANGE_ME_TO_A_32+_CHAR_RANDOM_SECRET
JWT_ACCESS_TOKEN_MINUTES=15
AUTH_BOOTSTRAP_API_KEY=CHANGE_ME_BOOTSTRAP_KEY
ADMIN_BOOTSTRAP_EMAIL_0=admin@example.com

# Web frontend -> backend integration
# This is used by the Next.js BFF for AppOnly endpoints (e.g. live game).
WEB_TRN_BACKEND_API_KEY=CHANGE_ME_WEB_APPONLY_KEY

# Hangfire dashboard basic auth (WebAdminPortal)
HANGFIRE_DASHBOARD_USERNAME=admin
HANGFIRE_DASHBOARD_PASSWORD=CHANGE_ME_STRONG_PASSWORD

# GHCR auth for pulling private package images
GHCR_USERNAME=luisgon-dev
GHCR_TOKEN=CHANGE_ME_GHCR_PAT_WITH_READ_PACKAGES
